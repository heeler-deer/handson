auto cmp = [](pair<int, int>& a,pair<int, int>& b) {
    return a.second < b.second; 
    // 按照第二个元素的大小排序
};

// 定义priority_queue，并传入比较函数
priority_queue<pair<int, int>, vector<pair<int, int>>, decltype(cmp)> pq(cmp);